# Vercel Environment Variables
# Add at: Project → Settings → Environment Variables
# Apply to: Production, Preview, Development
# Use the SAME values as your local .env

# Required for volunteer/contact forms on Vercel:
# DATABASE_URL must use port 6543 with ?pgbouncer=true&connection_limit=1
# Copy from your .env file

DATABASE_URL=
ADMIN_PASSWORD=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=

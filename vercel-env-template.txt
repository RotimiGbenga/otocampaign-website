# Vercel Environment Variables
# Add at: Project → Settings → Environment Variables
# Apply to: Production, Preview, Development
# Use the SAME values as your local .env

# CRITICAL for volunteer/contact forms:
# DATABASE_URL must match .env exactly - port 6543, pgbouncer=true, connection_limit=1
# App now uses @prisma/adapter-pg for better Vercel/Supabase compatibility

DATABASE_URL=
ADMIN_PASSWORD=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=

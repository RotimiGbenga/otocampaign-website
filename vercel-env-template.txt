# Vercel Environment Variables
# Add at: Project → Settings → Environment Variables
# Apply to: Production, Preview, Development
# Use the SAME values as your local .env

# CRITICAL: Vercel has NO IPv6 - must use pooler host (aws-0-*.pooler.supabase.com)
# NOT db.supabase.co - that uses IPv6 only and Vercel cannot reach it (P1001)
# Get from: Supabase Dashboard → Connect → Transaction pooler

DATABASE_URL=
ADMIN_PASSWORD=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=

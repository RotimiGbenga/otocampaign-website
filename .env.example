# Admin authentication – password for /admin/login
ADMIN_PASSWORD="your-secure-admin-password"

# Email notifications (volunteer + contact)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-username"
SMTP_PASS="your-smtp-password"
ADMIN_EMAIL="admin@campaign.com"

# Optional: From address (defaults to ADMIN_EMAIL)
# SMTP_FROM="noreply@campaign.com"

# Database - Supabase PostgreSQL (required for Vercel serverless)
# Supabase Dashboard → Project Settings → Database → Connection string
#
# IMPORTANT: For Vercel, use the TRANSACTION pooler (port 6543).
# WRONG: db.[PROJECT_REF].supabase.co:6543 (db host does not support 6543)
# RIGHT: [REGION].pooler.supabase.com:6543
#
# Transaction pooler (serverless/Vercel - required for writes):
# postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
#
# Get your exact string: Supabase Dashboard → Connect → Transaction pooler
# Add pgbouncer=true and connection_limit=1 for Vercel serverless
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&connect_timeout=30"
